/// Handles data migration for Hive boxes in the Ingest feature.
/// Ensures backward compatibility when schema changes occur.
class CaptureMigrationHelper {
  /// Performs migration from one version to another.
  /// Currently a no-op as no migration is needed for this version.
  void performMigration() {
    // No migration logic required for current version
  }
}
