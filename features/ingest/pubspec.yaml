name: cascade_flow_ingest
description: Ingest feature slice (domain, data, presentation).
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.9.2 <4.0.0'
  flutter: '>=3.19.0'

resolution: workspace

dependencies:
  cascade_flow_core:
    path: ../../packages/core
  cascade_flow_infrastructure:
    path: ../../packages/infrastructure
  collection: ^1.18.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.1
  flutter_secure_storage: ^9.2.4
  hive_ce: ^2.14.0
  meta: ^1.11.0
  record: ^6.1.2
  riverpod: ^3.0.1
  speech_to_text: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  path_provider_platform_interface: ^2.1.2
  plugin_platform_interface: ^2.1.8
  very_good_analysis: ^10.0.0

flutter:
  uses-material-design: true
